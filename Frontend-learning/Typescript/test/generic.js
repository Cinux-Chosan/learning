var Model = /** @class */ (function () {
    function Model() {
    }
    Model.fromJson = function (o) { };
    return Model;
}());
var someList = [];
function replaceModelItem(t, k) {
    // if (!(t instanceof Model)) {
    //   t = ;
    // }
    // const index = someList.indexOf(t);
    // someList[index] = t;
}
