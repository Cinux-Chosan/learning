1、中断通常对应着一个中断号，内核通过这个中断号查找相应的中断服务程序，并且调用这个程序响应和处理中断。
2、中断不在进程上下文中运行，而在一个与所有进程都无关的、专门的中断上下文中运行。这样可以保证中断能第一时间响应请求。
3、单内核：所谓单内核就是把它从整体上作为一个单独的大过程来实现，同时也运行在一个单独的地址空间上。因此，这样的内核通常以单个的静态二进制文件的形式存放于磁盘中。所有内核服务都在这一个大内核地址中运行。
   微内核：微内核的功能被划分为多个独立的过程，每个过程叫做一个服务器。所有的服务器都保持独立并运行在自己的地址空间上。
